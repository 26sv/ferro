# GESTIONALE FORMAZIONE

Gestionale per la formazione.

Repo appena aperta: non c'è ancora codice. Lo stack non è stato scelto,
quindi per ora qui dentro ci sono solo questo README e il `.gitignore`.
